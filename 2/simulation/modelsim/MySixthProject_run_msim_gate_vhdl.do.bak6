transcript on
if {[file exists gate_work]} {
	vdel -lib gate_work -all
}
vlib gate_work
vmap work gate_work

vcom -93 -work work {MySixthProject.vho}

vcom -93 -work work {C:/altera/13.0sp1/lab6/simulation/modelsim/ctrlLogic_tb.vhd}

vsim -t 1ps -L cycloneii -L gate_work -L work -voptargs="+acc"  ctrlLogic_tb

add wave *
view structure
view signals
run -all
